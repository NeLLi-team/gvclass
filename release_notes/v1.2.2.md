# GVClass v1.2.2

## Highlights
- Fixed HPC/runtime failures caused by ephemeral Prefect API startup in restricted environments.
- Simplified runtime execution path to run pipeline tasks directly while preserving parallel query processing.
- Published updated Apptainer image tag `library://nelligroup-jgi/gvclass/gvclass:1.2.2`.

## Versioning and distribution
- Updated software and CLI version strings to `v1.2.2`.
- Updated default container image references (`gvclass-a`, Apptainer, Docker, Shifter) to `1.2.2`.
- Updated README version badge and release section for `v1.2.2`.

## Database notes
- Database version updated to `v1.2.2`.
- Primary database source updated to `resources_v1_2_2.tar.gz` on Zenodo.
- Added taxonomy lookup cleanup for Nucleocytoviricota/PHAGE label consistency in `gvclassFeb26_labels.tsv`.

## Validation
- Verified local example pipeline run (`pixi run gvclass example ...`) completes and writes expected summary outputs.
- Verified `gvclass_1.2.2.sif` runs example data successfully and writes expected summary outputs.

## Archive
- Previous release notes are archived as `release_notes/v1.2.1.md`.
